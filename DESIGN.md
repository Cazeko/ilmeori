# 일머리 — 디자인 시스템

이 문서는 **토큰이 무엇을 뜻하는지**와 **어떤 자리에 무엇을 쓰는지**를 정한다.
색값 목록은 `src/app/globals.css` 의 `@theme` 이 원본이고, 여기서는 그 값에
**등급(rank)** 을 붙인다.

---

## 0. 태도 — 이 제품은 자기가 뽑아내는 문서처럼 생긴다

일머리가 마지막에 내놓는 것은 화면이 아니라 **종이**다. 별지 제12호서식,
HWPX, 기안문. 그런데 지금 앱의 껍데기는 그 종이와 아무 상관 없는
둥근 카드다. 한 제품 안에 두 개의 언어가 있다.

| | 지금 쓰는 언어 | 어디에 |
|---|---|---|
| 종이 | 흰 바탕, 검은 괘선, 각진 모서리, pt 단위 | `editor.css`, `print-sheet.tsx`, `approval-grid.tsx` |
| 카드 | `#fafafa`, 6px 둥글기, 1px 옅은 테두리 | 나머지 전부 (16개 파일, 34곳) |

**종이 쪽이 진짜다.** 세 가지 이유이고 셋 다 취향이 아니다.

1. 제품의 결과물이 종이다. 껍데기는 자기가 만드는 것을 닮아야 한다.
2. 「색이 너무 다채롭다」를 구조로 푼다. 종이는 무채색이다. 색이 규율이
   아니라 **기본값으로** 예외가 된다.
3. 「평평하다」를 제대로 푼다. 반사적으로 그림자를 넣게 되는데 그건 틀렸다.
   서식이 평평하지 않은 이유는 깊이가 아니라 **선 굵기**다 — 칸은 가는 선,
   테두리는 굵은 선, 제목 아래는 검은 선. 지금 이 앱의 껍데기에는
   **선 굵기가 한 가지뿐이다**(1px `gray-10`). 그것이 평평함의 정체이고,
   그림자로는 건드려지지 않는다.

> **한 줄로:** 일머리는 자기가 뽑아내는 문서처럼 생긴다 — 검은 선으로 그은
> 흰 서식, 그리고 그 방에서 유일한 색은 「늦었다」는 색뿐이다.

---

## 1. 진단 — 왜 AI가 만든 것처럼 보였나

1차 심사평은 이렇게 말했다. *「박스 레이아웃이나 글씨 크기 등이 AI가 만든
것이 티가 많이 남… 모두 평평하기 때문에 무엇이 핵심이고 무엇이 보조인지
시선이 안 감… 컬러가 너무 다채로워서.」*

홈 화면은 이미 고쳤다(`18a5de1`). 실눈 시험의 흐린 그림 두 장이 그것을
보여 준다 — 고치기 전에는 같은 무게의 덩어리 여럿, 고친 뒤에는 덩어리 하나.
명도 대비도 29건 전부 통과한다.

**그런데 나머지 여섯 화면은 그대로다.** 홈이 고쳐진 방식이 손으로 짠 것이라
토큰에 남지 않았기 때문이다. 토큰이 등급을 말할 줄 모르면, 다음 사람은
다음 화면에서 같은 상자를 또 집는다.

측정값으로 남긴다(현재 HEAD 기준):

| 사실 | 수 | 뜻 |
|---|---|---|
| `rounded-md border border-gray-10 bg-surface` | **34곳 / 16파일** | 화면마다 같은 상자를 크기만 바꿔 쓴다 |
| `text-h3 font-bold text-gray-90` | **17곳** | 홈 밖의 모든 제목이 한 등급이다 |
| `@theme` 의 간격 토큰 | **2개** (`sidebar`,`header`) | 간격에 체계가 없다 |
| `@theme` 의 그림자 토큰 | **0개** | 깊이 축이 없다. 실제 그림자는 임의값 1개를 2곳에 복사 |
| `@theme` 의 선 굵기 토큰 | **0개** | 종이 언어의 **주된 위계 축이 통째로 없다** |
| 본문(17) → 소제목(19) 비율 | **1.12배** | 2px 차이는 제목으로 읽히지 않는다 |
| `--text-h3` 와 `--text-body-lg` | 둘 다 **19px** | 이름 둘, 값 하나 |
| 쓰이는 간격 단계 | **19가지** (2px 반단계 136회) | 사다리가 아니라 그때그때 |
| `krds-tokens.css` | **49,939 바이트, 참조 0건** | 렌더 막는 CSS의 44%가 죽은 코드 |

핵심은 이것이다. 이 화면은 **요란해서** AI 같아 보이는 게 아니다.
**고르게 무난해서** 그렇다 — 모든 요소가 저마다 정당한데, 어떤 요소도
다른 요소에 종속되어 있지 않다. 종속(subordination)이 토큰에 없으면
다음 화면에서 또 사라진다.

---

## 2. 색 — 손대지 않는다

화성특례시 BI 파랑 `#004696`, 주황 `#dc6e2d`, KRDS 파생 회색 13단,
그리고 `npm run test:contrast` 가 지키는 29쌍. **이 부분이 이 저장소에서
AI티가 가장 적은 자산이다.** 출처가 있고, 실측값이 있고, 시험이 있다.
값은 그대로 둔다.

바꾸는 것은 **쓰는 규칙**뿐이고, 그것은 이미 `globals.css` 주석에 있는 말을
토큰 이름으로 승격시키는 일이다.

| 갈래 | 색 | 자리 |
|---|---|---|
| 정보 전부 | 먹색 `gray-*` | 글, 선, 표 |
| 누를 수 있는 것 · 지금 여기 | HS Blue `primary` | 링크, 켜진 것 |
| 지금 문제인 것 | 빨강 `overdue` | 지연·반려 **둘뿐** |
| 내가 움직여야 하는 것 | HS Orange `accent` | 내 차례·인계·임박 |

**한 화면 색 예산: 무채색 + 위 세 갈래 중 최대 2개.**

### 2.0 아바타 — 사람마다 색을 주지 않는다. 「나」에게만 준다

before 는 아바타가 보라·파랑·초록·주황·청록이었고, 그것이 「알록달록하다」의
가장 큰 몫이었다. 지금은 전부 회색인데, 외부 평가 두 건이 **똑같이**
「과하게 무채색이라 구분이 안 된다」고 지적했다. 각각 파스텔 톤과
2~3색 제한을 처방했는데, **둘 다 「사람마다 색」이라는 전제를 깔고 있고
그 전제가 틀렸다.**

- 24px 원을 겹쳐 놓은 자리에서 색으로 사람을 구별하는 것은 원래 안 된다.
- 인원이 20명이 되면 색이 반복되어 **거짓 정보**가 된다.
- 색맹 사용자에게는 처음부터 없는 정보다. 공공 시스템에서 색만으로
  나르는 정보는 두면 안 된다.

이 카드에서 아바타가 답하는 질문은 「누구누구인가」가 아니라
**「이거 내 일인가」** 다.

> **내 아바타 하나에만 색을 준다(HS Orange 계열 = 내가 움직여야 하는 것).
> 나머지는 전부 `bg-gray-10 text-gray-70`(7.07:1).**

색 하나로 끝나고, 인원이 늘어도 안 깨지고, 이미 있는 4갈래 언어에 그대로 붙는다.
이름은 색이 아니라 `title`·상세 화면이 나른다.

### 2.1 지금 이 예산을 넘는 자리 (고칠 것)

- `src/styles/editor.css` — 팔레트 밖 hex **10개**. 협업 커서 `#8a3ffc`(보라)
  `#b8355f`(자홍), 형광펜 `#fff3b0` `#d7f0d9` `#d6ecfb` `#fbd9e4`.
  이름 없는 색이 문서 화면에서 색 갈래를 **7개**까지 밀어 올린다.
  → 커서는 사람색 대신 **이름표 + 먹색 선**으로. 형광펜은 유지하되
  `--mark-*` 로 이름을 준다(사용자가 고르는 색은 팔레트가 아니다).
- `src/app/global-error.tsx` — 팔레트를 손으로 다시 적은 두 번째 사본
  (hex 9개, 글자크기 3개, 둥글기 2개). CSS 없이 뜨는 화면이라 인라인인 것은
  맞지만, **값이 갈라진다.** 빌드 때 토큰에서 생성하거나, 최소한 두 곳이
  같아야 한다는 시험을 붙인다.
- 쓰이지 않는 토큰 **8개** 삭제: `primary-40`, `gray-95`, `status-todo`,
  `status-todo-bg`, `status-todo-text`, `status-doing-text`, `status-review`,
  `status-done-bg`. 이름이 남아 있으면 반드시 다시 쓰인다.

---

## 3. 선 굵기 — 새 축, 그리고 위계의 주력

종이에서 위계를 만드는 것은 그림자가 아니라 선이다. 지금은 한 가지뿐이라
**이 축을 새로 만든다.** 이것이 이번 작업의 핵심이다.

```css
/* @theme 에 추가 */
--rule-hair:  var(--color-gray-20);   /* 1px — 표 안쪽 칸선, 목록 구분선 */
--rule-frame: var(--color-gray-40);   /* 1px — 판·구역의 바깥 테두리      */
--rule-head:  var(--color-gray-90);   /* 2px — 문서 제목 아래 검은 선     */
--rule-alarm: var(--color-status-overdue); /* 3px — 유일한 경보 모서리    */
```

| 이름 | 굵기 | 색 | surface 대비 (실측) | 자리 |
|---|---|---|---|---|
| `hair` | 1px | `gray-20` `#cdd1d5` | **1.47:1** | 표 칸선, 목록 구분선 |
| `frame` | 1px | `gray-40` `#8a949e` | **2.95:1** | 판의 바깥 테두리 |
| `head` | 2px | `gray-90` `#1e2124` | **15.50:1** | 문서·1등 제목 아래 |
| `alarm` | 3px | `overdue` `#de3412` | **4.03:1** | 지연 한 자리 |

참고로 지금 값과 후보들: `gray-10` **1.18** · `gray-20` 1.47 · `gray-30` 1.92 ·
`gray-40` 2.95 · `gray-50` 4.32.

지금 전부 `gray-10`(대비 **1.18** — 거의 안 보인다)인 테두리를 `frame` 으로
올리면, 판이 처음으로 바탕에서 **모서리로** 구분된다. 판 색(`#fafafa`)과
바탕(`#f0f1f2`)의 명도차 1.08 을 건드리지 않고도 그렇게 된다 —
그 1.08 은 일부러 낮게 잡은 값이고 시험이 상한 1.2 로 지키고 있으므로,
**대비는 채움이 아니라 선에서 벌어야 한다.**

> **`frame` 의 2.95 는 3:1 바로 아래다.** 숨기지 않고 적어 둔다.
> WCAG 1.4.11(비문자 3:1)은 「이해에 필요한」 요소에 걸리는 조항이고 판의
> 장식 테두리는 거기 해당하지 않는다고 보아 `gray-40` 을 고른다 —
> 모든 판에 `gray-50`(4.32)을 두르면 입력칸 테두리와 같은 굵기가 되어
> 뜻이 충돌한다. 엄격히 3:1 을 넘기고 싶으면 `gray-50` 으로 올리되,
> 그때는 입력칸 테두리를 한 단계 더 올려 구분해야 한다.
> 어느 쪽을 고르든 `contrast.test.mjs` 에 이 쌍을 **새 검사로 추가**한다.

---

## 4. 글자 — 아래는 그대로, 위를 벌린다

지금 사다리는 13 / 15 / 17 / 19 / 24 / 32 이고, 아래 네 칸의 비율이
1.15 · 1.13 · 1.12 다. **2px 차이는 눈이 등급으로 읽지 못한다.**
17px 본문 옆의 19px 제목은 그냥 굵은 본문이다. 이것이 「평평하다」의
산술적 뿌리다.

데이터를 나르는 아래 세 칸(13/15/17)은 전체 선언의 84%를 차지하고 잘
동작한다 — **건드리지 않는다.** 위만 벌린다.

```css
--text-body-xs: 13px;   /* 메타·캡션            그대로 */
--text-body-sm: 15px;   /* 보조                 그대로 */
--text-body:    17px;   /* 본문·데이터          그대로 */
--text-h3:      21px;   /* 구역 제목      19 → 21 */
--text-h2:      27px;   /* 1등 대상 제목  24 → 27 */
--text-h1:      34px;   /* 문서 제목      32 → 34 */
--text-figure:  46px;   /* 화면당 하나의 숫자   ← 새로 */
```

비율: 1.15 · 1.13 · **1.24** · 1.29 · 1.26 · 1.35. 아래는 촘촘하고 위로
갈수록 벌어진다. 17 → 21 이 1.12에서 **1.24** 로 바뀌는 것이 이 문서에서
가장 중요한 숫자 하나다.

**삭제한다:**

- `--text-body-lg` (19px) — `--text-h3` 와 값이 같다. 이름 둘에 값 하나면
  둘 다 뜻을 잃는다. 쓰는 곳 2군데.
- `text-[11px]` (7곳), `text-[12px]` (1곳) — 13px 캡션 바닥 아래다.
  `--text-body-xs` 로 올린다.

**`--text-figure` 를 새로 두는 이유:** 지금 32px 이 두 가지 일을 겸한다 —
화면 이름표(「업무 보드」)와 히어로의 숫자(「9일 지남」). 그래서
**아무 뜻 없는 이름표가 화면에서 가장 큰 것**이 되는 화면이 생긴다.
숫자에 따로 등급을 주면 이름표는 자동으로 물러난다.

---

## 5. 판 등급 — 전파를 만드는 장치

지금 `card.tsx` 에는 등급이 셋 있는데(`hero`/`default`/`quiet`),
`hero` 는 **한 번도 안 쓰이고** `quiet` 는 **홈에서만** 쓰인다. 등급이
있는데 안 쓰이면 없는 것이다. 세 등급을 앱 전체의 **유일한 판 어휘**로
못박는다.

| 등급 | 상자 | 제목 | 화면당 |
|---|---|---|---|
| **문서** `doc` | `bg-white` · 각진 모서리 · 위쪽 `rule-head`(2px 먹) | `text-h1` | **최대 1개** |
| **판** `panel` | `bg-surface` · `rule-frame` 1px · `radius-sm` 4px | `text-h3` | 여러 개 |
| **여백** `quiet` | 상자 없음 | `text-body-sm` `gray-60` | 여러 개 |

**규칙: 한 화면에 「문서」는 하나다.** 이 한 줄이 실눈 시험이 재는 것과
같은 말이고, 홈이 이미 통과한 것이며, 나머지 여섯 화면이 어긴 것이다.

화면별로 무엇이 「문서」인가 — 이것이 이 시스템의 진짜 내용이다:

| 화면 | 「문서」(1등) | 지금은 |
|---|---|---|
| 홈 | 가장 급한 일 한 건 | ✅ 이미 그렇다 |
| 업무 보드 | 지연 카드 | ❌ 「업무 보드」 이름표가 32px로 1등 |
| 업무 상세 | 업무 제목 + 상태 바꾸기 | ⚠️ 제목은 맞으나 조작기가 15px |
| 결재함 | 「지금 내 차례」 줄 | ❌ 「결재함」 이름표가 1등, 내 차례는 13px 칩 |
| 결재 상세 | **결재란**, 그리고 상신/결재 단추 | ❌ 되돌릴 수 없는 상신이 15px |
| 인계·인수 | **별지 제12호서식 그 자체** | ❌ 서식은 `print:block` 이라 화면에 없다 |
| 로그인 | 계정 고르는 판 | ❌ 32px 마케팅 문구가 1등 |

인계 화면이 가장 아깝다. 이 제품에서 가장 강한 물건인 별지 제12호서식이
이미 잘 만들어져 있는데 `hidden print:block` 이라 **Ctrl+P 를 눌러야만
보인다.** 화면의 초안은 회색 말풍선 문단으로 따로 그린다.
→ 서식을 화면의 「문서」로 올리고, 근거 칩은 여백(marginalia)으로 뺀다.

### 5.1 「문서」는 **콘텐츠**여야 한다 — 크롬이 아니라

> **핵심과 보조를 구분하라는 것이지, 핵심을 무조건 크게 만들라는 것이 아니다.**

업무 보드에서 실제로 벌어진 일을 남긴다. 「위계가 없다」를 풀겠다고
상단에 **거대한 경고 배너**(약 125px, 빨간 숫자 「2」)를 얹었다.
실눈 시험은 통과했다 — 덩어리가 하나 생겼으니까. 그런데 그 덩어리가
**눌러야 할 일이 아니라 개수를 세는 상자**다.

무엇이 잘못됐나.

1. 화면에서 가장 큰 글자가 **합계**다. 홈 화면 주석이 금지한 바로 그것이다 —
   *"통계 숫자를 위에 크게 올리는 구성은 일부러 피했다. 하루를 시작하면서
   알고 싶은 건 합계가 아니라 다음 할 일이다."* 홈에서 막은 것을 보드에서 했다.
2. 같은 사실이 화면에 **세 번** 있다 — 배너 · `지연만` 필터 칩 ·
   대기 컬럼의 `지연 2` 뱃지.
3. `works/page.tsx:152` 주석은 *"이 화면의 1등은 아래 「기한이 지난 업무」다"*
   라고 적어 놓고, 정작 1등으로 세운 것은 **업무가 아니라 배너**다.

**규칙으로 못박는다: 화면의 「문서」는 사용자가 누를 대상이어야 한다.**
요약 배너·통계 타일·경고 상자는 아무리 커도 「문서」가 아니라 **여백 등급**이다.

보드의 옳은 모양:

| | 등급 | 처리 |
|---|---|---|
| 지연 배너 | 여백 | 한 줄(약 44px). 거대 숫자 없앤다 |
| **지연 카드** | **문서** | 제목 `text-h3`(21px) · 왼쪽 `rule-alarm` · 안쪽 여백 한 칸 더 |
| 나머지 카드 | 판 | 제목 `text-body`(17px) |
| 페이지 이름표 | 여백 | `text-body`(17px) `gray-90`. 32px 로 되돌리지 않는다 |

#### 같은 결함이 결재함에도 있었다 (2026-08-23 실측)

보드만의 일이 아니었다. 결재함에도 **똑같은 모양**의 문서 등급 배너가 있었고
(46px 주황 숫자를 단 흰 종이), 한 가지가 더 나빴다 — **그 배너의 링크가
`/approvals` 였다. 지금 보고 있는 바로 그 화면이다**(`box` 의 기본값이 `todo`).
화면에서 가장 크고 가장 눈에 띄는 것이 눌러도 아무 일이 없었다.
같은 사실은 여기서도 세 곳에 있었다 — 배너 · 왼쪽 칸의 「대기 N」 배지 ·
목록 머리의 「대기함 N건」.

배너를 걷어내고 문서 등급을 **결재 목록 그 자체**로 옮겼다(대기함일 때만).
「지금 내 차례」 줄은 다른 칸을 보고 있을 때만 한 줄로 남는다.

한 가지가 더 걸렸다. 데모 모드의 「지금은 읽기 전용입니다」가 채운 파란 판이라
**그 화면에서 가장 무거운 덩어리**였고, 서명할 문서 목록보다 위에 있었다.
안내를 지우지 않고 무게만 맞춘다 — 늘 참인 상태는 사건이 아니고,
「시연용 가상 데이터」는 머리 줄이 이미 늘 말한다. 다만 **폼 화면(새 업무·결재
올리기)에서는 판을 그대로 둔다.** 거기서는 곧 시작할 동작을 막는 말이라
먼저 읽혀야 한다.

#### 규칙을 한 번 더 벼린다 — 문서는 한 **종류**이지 한 **개**가 아니다

「한 화면에 문서는 하나」를 글자 그대로 읽으면 보드에서 막힌다. 지연된 업무가
두 건이면 문서 등급 요소도 두 개다.

그 규칙이 실제로 막으려는 것은 **서로 다른 종류가 둘 다 1등을 주장하는 일**이다
— 배너 하나 + 카드 여럿처럼. 그러니 이렇게 적는다.

> **문서 등급은 화면당 한 「종류」다. 개수는 데이터가 정한다.**
> 홈은 그 종류가 「가장 급한 일」이라 언제나 한 개이고, 보드는 「지연된 업무」라
> 0개일 수도 세 개일 수도 있다. 0개인 날 화면에 1등이 없는 것은 옳다.

그리고 등급을 올리는 데 쓰는 것은 세 가지뿐이다 — **제목 크기 · 뜻 있는 선 ·
안쪽 여백 한 칸.** 카드에 `doc` 겉모양(흰 종이 + 위쪽 2px 먹선)까지 씌우지는
않는다. 위 먹선은 「여기서 문서가 시작한다」는 뜻인데 열 안에 든 카드 두 장이
그 말을 하면 열의 윗변과 다투고, 흰 종이는 판과 대비 1.02 라 아무것도 하지
않으면서 규칙만 늘린다.

### 5.2 카드 안의 대비는 **크기·굵기**로만 낸다

제목과 메타(부서·마감·참여자)의 대비를 키우려고 메타를 더 흐리게 하고 싶어지는데,
**`gray-50` 은 surface 위에서 4.32:1 이라 본문 기준 4.5 에 미달한다**(시험이 재고 있는 값).
메타 글자색은 `gray-60` 아래로 못 내려간다. 대비는 색이 아니라 **크기와 굵기**에서 번다.

구조 반복은 문제가 아니다 — 카드마다 상태 → 제목 → 부서 → 참여자 순서가 같은 것은
훑어 읽기의 전제 조건이다(Krug). AI 티는 **구조 반복**이 아니라 **무게 반복**에서 온다.

---

## 6. 간격 — 4px 사다리로 잠근다

지금 19가지 단계를 쓰고, 그중 2px 반단계(`0.5 1.5 2.5 3.5 5.5`)가 **136회**다.
`work-card.tsx` 는 주석에 「4·8·12만 쓴다」고 적어 놓고 **자기 파일 안에서
이미 어기고 있다**(`gap-1.5`, `px-1.5 py-0.5`).

```
4 · 8 · 12 · 16 · 24 · 32 · 48        (Tailwind: 1 2 3 4 6 8 12)
```

- 반단계(`*-0.5 *-1.5 *-2.5 *-3.5 *-5.5`)는 **쓰지 않는다.**
  예외는 배지 안쪽(`px-1.5 py-0.5`) 하나뿐이고, 그건 `--pad-chip` 으로
  이름을 준다.
- 판 안쪽 여백은 등급이 정한다: 문서 `24`, 판 `16`, 여백 `0`.
  지금은 123개 상자가 **31가지 조합**을 쓴다.

---

## 7. 깊이와 둥글기 — 줄인다

**그림자: 없앤다.** 지금 앱 전체에 그림자는 하나뿐이고, 그마저 토큰이 아닌
임의값을 두 곳에 복사해 둔 것이다(`card.tsx:41`, `urgent-hero.tsx:72`).
종이는 뜨지 않는다. 히어로의 우위는 **크기 + 여백 + `rule-head`** 로 낸다.
`--shadow-*` 토큰은 만들지 않는다 — 만들면 쓰인다.

**둥글기: 5단 → 2단.**

```css
--radius-sm: 4px;   /* 단추·입력칸·칩 */
/* 문서·표·판은 0. 서식에 둥근 모서리는 없다. */
```

`--radius-md`(6px, **57곳**) · `lg` · `xl` 를 지운다. 6px 둥근 상자 34개가
「AI가 만든 박스 레이아웃」이라는 말을 들은 그 물건이다.

---

## 8. 옮기는 순서

값만 바뀌는 것과 손이 가는 것을 나눈다.

**1단계 — 공짜 (반나절)**
1. `globals.css:21` 의 `@import "../styles/krds-tokens.css"` 삭제 + 파일 삭제.
   참조 0건, 49,939바이트, 렌더 막는 CSS의 44%. 화면은 1픽셀도 안 바뀐다.
2. 안 쓰는 토큰 8개 + `--radius-xl` 삭제.
3. `--text-body-lg` 삭제(2곳), `text-[11px]`·`text-[12px]` → `text-body-xs`(8곳).

**2단계 — 토큰 값 (하루)**
4. 글자 사다리 위 세 칸 조정 + `--text-figure` 추가.
5. `--rule-*` 네 개 추가.
6. `border-gray-10` → `rule-frame` 일괄(87곳). 이때 명도 시험을 다시 돌린다.

**3단계 — 등급 전파 (화면당 반나절, 여섯 화면)**
7. `card.tsx` 를 문서/판/여백 세 등급으로 다시 쓰고, `rounded-md border
   border-gray-10 bg-surface` **34곳을 등급 호출로 바꾼다.**
8. 화면마다 §5 표의 「문서」를 하나 세운다. 이름표를 1등에서 내린다.
9. 인계 화면: `print-sheet` 를 화면으로 올린다.

**4단계 — 못 돌아가게 막기**
10. `test:squint` 를 `check` 에 넣는다(지금은 스크립트만 있고 `check` 밖).
    playwright·서버가 필요하니 `check:visual` 로 따로 묶어도 된다.
11. 간격 반단계와 팔레트 밖 hex 를 잡는 lint 시험을 붙인다 —
    이 저장소는 이미 디자인 규칙을 시험으로 지키는 문화가 있다
    (`contrast.test.mjs`, `squint.test.mjs`). 같은 방식으로 늘린다.

---

## 9. 지키는 법

| 시험 | 재는 것 | 상태 |
|---|---|---|
| `test:contrast` | **38쌍** 명도 대비, `globals.css` 직접 읽음 | ✅ `check` 안 |
| `test:design` | 반단계 · 날 hex · 팔레트 갈림 · 죽은 `var()` · hover 가 경보선 덮기 · `loading.tsx` · 지운 토큰 부활 · **문서 표식 누락** (8검사) | ✅ `check` 안 |
| `test:squint` | 8px 블러 · 지배도 · **1등 칸의 자리** (5화면) | ✅ `check:visual` (playwright·서버 필요) |

`test:spacing`·`test:palette` 로 따로 만들려던 것은 `test:design` 한 파일로
합쳤다 — 셋 다 「소스를 읽어 규칙을 세는」 같은 종류라 파일을 나눌 이유가 없었다.

실눈 시험은 다섯 화면을 **다 재고, 무게는 홈에서만 판정한다.** 지배도는
「바탕 위에 덩어리 하나가 놓였는가」를 묻는데, 칸반(균등 격자)과 인계(서식이
화면 전체)에서는 그 물음 자체가 성립하지 않는다. 문턱을 낮추거나 화면을
문턱에 맞추는 대신 **물음이 성립하는 곳에만 건다.** 자리 검사는 반대로
모든 화면에 건다 — 화면의 모양에 흔들리지 않는 물음이기 때문이다.

### 9.1 ⚠ 실눈 시험에는 구멍이 있다 — 지배도만으로는 부족하다

업무 보드가 이 구멍을 실제로 통과했다. **지배도 ≥ 1.5 는 「상단에 큰 상자를
하나 얹으면」 만족된다.** 그리고 큰 상자를 얹는 것이 정확히 AI 가 하는
실패 방식이다 — 「중요한 걸 강조해줘」라고 하면 거대한 숫자와 아이콘 상자를
화면 위에 툭 던져 놓는 그 패턴이다.

즉 **시험이 틀린 이유로 통과했다.** 위계가 생겨서가 아니라 배너가 커서였다.

시험에 조건을 하나 더 단다.

> **지배도 1등 칸이 「문서」 등급 요소 위에 있어야 한다.**
> 1등 칸이 페이지 상단 크롬(머리글·배너·필터 줄) 영역이면 **실패**로 친다.

구현은 간단하다. 채점할 때 「문서」로 표시한 요소의 사각형을
`getBoundingClientRect()` 로 받아, 지배도 1등 칸의 중심이 그 안에 들어오는지
본다. 안 들어오면 숫자가 아무리 높아도 실패다.

#### 그 「간단한 구현」이 틀렸다 — 만들어 재 보고 고친 자리 (2026-08-23)

위 문단대로 **문서 사각형 안에 1등 칸의 중심이 들어오는가**로 짰더니
다섯 화면 중 셋이 실패했다. 들여다보니 화면이 아니라 시험이 틀렸다.

이 지표가 세는 무게는 `|칸 평균 밝기 − 중앙값|` 이고, 그 값을 실제로 밀어
올리는 것은 **잉크의 양**이다. 홈의 1등 칸은 히어로가 아니라 화면 아래쪽의
촘촘한 목록 두 벌이었다(1등 칸 y=882, 히어로 윗변 y=329) — 히어로는 넓은 흰
판이라 면적당 잉크가 적다. 목록이 촘촘한 것은 옳은 설계다. 그것을 옅게 만들어
시험을 통과시키는 것은 **시험이 화면을 망가뜨리는** 일이고, 이 문서가 §9.1 을
쓴 이유가 정확히 그 반대였다.

그래서 §9.1 이 원래 적어 둔 **부정형**으로 되돌린다.

> **가장 무거운 칸이 문서에 한 픽셀도 닿지 않으면서 문서보다 위에 있으면 실패.**

「문서 안에 있는가」(긍정형)가 아니라 「문서 위쪽 크롬에 있는가」(부정형)다.
판정은 칸의 **중심이 아니라 아랫변**으로 한다 — 격자가 912px 를 네 줄로
자르므로 한 칸이 228px 이고, 처음 판정에서 결재함이 실패한 차이는 **71px**
이었다. 자가 분해하지 못하는 눈금 사이를 읽고 실패를 선언하면, 그다음에
벌어지는 일은 사람이 눈금에 맞춰 화면을 미는 것이다.

거칠지만 막으려던 것은 정확히 잡는다. 예전 업무 보드는 배너 + 조건 폼 + 칩
줄로 문서 위에 370px 을 쌓고 있었다. **확인도 했다** — 보드에 300px 짜리 가짜
배너를 주입하니 문서 윗변이 y=354 → y=674 로 밀리고 1등 칸이 상단으로 옮겨
가면서 통과가 실패로 뒤집혔다. 통과만 하는 시험은 시험이 아니다.

표식은 `data-rank="doc"` 이고, `Card`·`CardPad` 는 스스로 붙인다. 손으로
`CARD_SURFACE.doc` 를 가져다 쓴 자리(7곳)에 표식이 빠지면 시험이 실패하는
것이 아니라 **조용히 아무것도 안 잰다.** 그래서 `test:design` 이 둘이 붙어
있는지 따로 감시한다.

이 조건이 없으면 다음 화면에서도 배너를 얹어서 통과시키게 된다.
숫자를 만족시키는 가장 쉬운 방법이 잘못된 방법이면, 그 시험은 잘못된
방법을 가르친다.

---

## 10. 하지 않을 것

- 그라디언트, 글래스모피즘, 큰 사진 히어로. 하루 여덟 시간 띄워 두는
  업무 창이다.
- 다크 모드. 지금 `dark:` 가 한 곳도 없다. **안 한다고 여기 적어 둔다** —
  적어 두지 않으면 누군가 절반만 만든다.
- 그림자 토큰. §7 참조.
- 색을 늘리는 것. 예산은 무채색 + 최대 2갈래다.
- 팔레트 값 변경. §2 참조.

---

## 11. 이미 있는 것 (다시 만들지 말 것)

이 저장소는 디자인 규칙을 **시험으로** 지키는 문화가 이미 있다. 새 시스템은
그 위에 얹는다.

| 자산 | 상태 |
|---|---|
| `test:contrast` — 29쌍 실측, `globals.css` 직접 읽음 | ✅ `check` 안. **29/29 통과** |
| `test:squint` — 8px 블러 지배도 | ✅ 스크립트 있음 (`check` 밖) |
| 화성시 BI 출처 + KRDS 파생 + 상호작용 상태 파생 규칙 | ✅ 주석에 근거까지 |
| 4갈래 색 언어 (먹·파랑·빨강·주황) | ✅ 정해져 있음. 전파만 남음 |
| `z-index` 5층 규약 | ✅ 지켜짐 (`z-[9999]` 0건) |
| 움직임 3개 + `prefers-reduced-motion` | ✅ |
| `focus-visible` 제거 안 함, 건너뛰기 링크, `word-break: keep-all` | ✅ |
| 44px 터치 목표 (`min-h-11` 51곳), `aria-*` 321곳 | ✅ |
| 스크립트 끈 상태 브라우저 시험 | ✅ 드묾 |
| 인쇄 CSS (`@layer` 밖, A4, 별지 제12호서식) | ✅ 제품의 가장 강한 물건 |
| 홈 화면 위계 (`urgent-hero`, 등급 나눔) | ✅ 이미 통과. **본보기로 쓴다** |

---

## 12. 이번 범위 밖 (일부러 미룸)

| 미룬 것 | 이유 |
|---|---|
| 다크 모드 | §10. 안 한다고 못박는 편이 반쪽짜리보다 낫다 |
| 새 서체 도입 | Pretendard GOV 400/700 은 접근성 체크리스트가 묶은 값이다. 굵기 축은 못 쓴다는 전제로 설계했다 |
| 편집기 내부 타이포 | `pack.ts` 의 HWPX 내보내기와 pt 단위로 물려 있다. 건드리면 파일이 깨진다 |
| 아이콘 세트 정리 (90종) | 실물이지만 토큰 문제가 아니다. 등급 정리 뒤 따로 |
| 로딩·오류 경계 | 디자인이 아니라 구조. §13 의 T5 로 넘긴다 |

---

## 13. 구현 과제

우선순위: **P1** 은 이번에 끝낸다 · **P2** 는 같은 갈래에서 · **P3** 은 나중.

> **T1 ~ T14 전부 들어갔다.** 다만 넷은 세운 계획과 다르게 끝났고, 그 어긋남이
> 계획보다 쓸모 있는 기록이라 여기 적는다.
>
> | 과제 | 계획 | 실제 |
> |---|---|---|
> | T5 | `loading.tsx` 추가 | **넣었다가 뺐다.** 스크립트 없는 브라우저에서 본문이 통째로 사라진다(React 스트리밍은 인라인 스크립트로 조각을 옮긴다). `error.tsx` 만 남고, `test:design` 이 `loading.tsx` **없음**을 지킨다 |
> | T4 | 7화면 전부 지배도 ≥ 1.5 | 재 보니 지표가 칸반·서식에서 성립하지 않는다. 목표가 아니라 **자를 고쳤다** — §9 |
> | T9 | `test:spacing` + `test:palette` 두 파일 | 한 파일(`test:design`, 8검사)로 합쳤다 |
> | T13 | 1등 칸이 문서 **안에** | 잉크 밀도 때문에 틀린 물음이었다. 부정형으로 뒤집었다 — §9.1 |

- [x] **T1 (P1, human: ~30min / CC: ~5min)** — `globals.css` — 죽은 KRDS 토큰 파일 삭제
  - 근거: `krds-tokens.css` 49,939바이트, `var(--krds-*)` 외부 참조 **0건**, 렌더 막는 청크의 44%
  - 파일: `src/app/globals.css:21`, `src/styles/krds-tokens.css`
  - 확인: `npm run build` 후 청크 크기 비교, 화면 diff 0
- [x] **T2 (P1, human: ~2h / CC: ~20min)** — `globals.css` — `--rule-*` 4단 추가, 글자 사다리 위 3칸 조정, `--text-figure` 추가, `--text-body-lg` 삭제
  - 근거: 선 굵기 토큰 0개 · 본문→소제목 비율 1.12 · `h3`와 `body-lg` 값 중복
  - 파일: `src/app/globals.css`
  - 확인: `npm run test:contrast` (새 `rule-frame` 쌍 추가 후 통과)
- [x] **T3 (P1, human: ~1d / CC: ~1h)** — `card.tsx` — 문서/판/여백 3등급으로 재작성
  - 근거: `hero` 0회 사용, `quiet` 1파일만 사용, 등급이 있는데 안 쓰인다
  - 파일: `src/components/ui/card.tsx`
  - 확인: `variant` 별 스냅샷
- [x] **T4 (P1, human: ~2d / CC: ~2h)** — 6개 화면 — `rounded-md border border-gray-10 bg-surface` 34곳을 등급 호출로 교체, 화면마다 「문서」 하나 세우기
  - 근거: §5 표. 보드·결재함·인계에서 **이름표가 1등**이다
  - 파일: `works/page.tsx`, `approvals/page.tsx`, `approvals/[id]/page.tsx`, `handover/page.tsx`, `works/[id]/page.tsx`, `login/page.tsx`
  - 확인: `npm run test:squint` 을 7화면으로 넓혀 전부 지배도 ≥ 1.5
- [x] **T5 (P2, human: ~4h / CC: ~30min)** — `app/(app)` — `loading.tsx` · `error.tsx` 추가
  - 근거: `loading.tsx` **0개**, route `error.tsx` **0개**, `Suspense` **0회**. 첫 페인트까지 전부 기다린다
  - 파일: `src/app/(app)/loading.tsx`, `src/app/(app)/error.tsx`
  - 확인: 느린 네트워크에서 골격이 먼저 뜨는지
- [x] **T6 (P2, human: ~2h / CC: ~15min)** — 인계 화면 — 별지 제12호서식을 화면의 「문서」로 올리기
  - 근거: 제품에서 가장 강한 물건이 `hidden print:block` 이라 Ctrl+P 전에는 안 보인다
  - 파일: `src/components/handover/print-sheet.tsx`, `src/app/(app)/handover/page.tsx`
- [x] **T7 (P2, human: ~1h / CC: ~10min)** — `approvals/[id]` — h1 반응형 단계 복원
  - 근거: `page-header.tsx:27` 은 `text-h2 sm:text-h1`, `approvals/[id]/page.tsx:155` 는 `text-h1` 고정. 320px에서 두 줄
- [x] **T8 (P2, human: ~3h / CC: ~20min)** — `editor.css` — 팔레트 밖 hex 10개 이름 주거나 제거
  - 근거: `#8a3ffc`(보라) `#b8355f`(자홍) 등이 문서 화면 색 갈래를 7개로 민다
- [x] **T9 (P3, human: ~2h / CC: ~15min)** — `tests/` — `test:spacing` · `test:palette` 추가, `test:squint` 를 `check:visual` 로 묶기
  - 근거: 간격 반단계 136회 · 팔레트 밖 hex. 지금은 아무것도 막지 않는다
- [x] **T10 (P3, human: ~1h / CC: ~10min)** — 안 쓰는 토큰 8개 + `--radius-xl` 삭제
  - 근거: 이름이 남으면 다시 쓰인다
- [x] **T11 (P1, human: ~3h / CC: ~25min)** — `works/page.tsx` — 지연 배너를 한 줄로 줄이고 「문서」 등급을 지연 카드로 옮기기
  - 근거: §5.1. 배너가 약 125px 를 먹고 화면 최대 글자가 **합계**다. 같은 사실이 세 곳에 있다
  - 파일: `src/app/(app)/works/page.tsx:181-215`, `src/components/work/work-card.tsx`
  - 확인: 실눈 시험 1등 칸이 **카드 영역** 안에 들어오는지 (§9.1)
- [x] **T12 (P2, human: ~1h / CC: ~10min)** — `avatar.tsx` — 「나」 아바타에만 색, 나머지 회색 유지
  - 근거: §2.0. 외부 평가 2건이 전부-회색을 동일하게 지적했으나 처방(파스텔 / 2~3색)은 둘 다 사람별 색을 전제해 틀렸다
  - 파일: `src/components/ui/avatar.tsx:28`
  - 확인: `npm run test:contrast` 에 「내 아바타」 쌍 추가 후 통과
- [x] **T13 (P1, human: ~2h / CC: ~20min)** — `tests/squint.test.mjs` — 1등 칸이 「문서」 요소 위에 있는지 검사 추가
  - 근거: §9.1. 지금 시험은 상단에 큰 상자를 얹으면 통과한다. 보드가 실제로 그렇게 통과했다
  - 확인: 현재 보드가 **실패**로 뒤집히는지 (뒤집혀야 정상이다)
- [x] **T14 (P2, human: ~30min / CC: ~5min)** — `works/page.tsx` — 컬럼 헤더에 작은 점, 페이지 이름표 17px `gray-90`
  - 근거: 굵은 4색 바 없이 컬럼 구분 복구. 15px 회색 이름표가 큰 파란 버튼 옆에서 캡션처럼 보인다

---

## 14. 외부 평가 두 건 대조 (2026-08-23)

같은 두 장(`squint-works-sharp-before/after.png`)을 두 모델에게 따로 물었다.
**거의 모든 항목에서 반대 결론이 나왔고, 딱 두 가지에서 일치했다.**
그 두 가지만 실제 결함으로 채택한다 — 서로 다른 취향이 만나는 자리가 신호다.

| 쟁점 | Gemini | GPT | 채택 |
|---|---|---|---|
| 전체 방향 | 1번으로 롤백 | 2번 유지 | **GPT** |
| 무채색 기조 | 과했다 | 공공 성격에 맞다 | **GPT** |
| 상태 5색 | 되살려라 | 절제가 좋다 | **GPT** |
| 그림자 | 넣어라 | 언급 없음 | **넣지 않음** |
| 다음 단계 | 그림자·파스텔 | spacing·타이포·일관성 | **GPT** |
| **상단 배너** | **제거** | **20~30% 축소** | ⚠️ **일치 → 채택** (§5.1) |
| **전부 회색 아바타** | **과하다** | **과하다** | ⚠️ **일치 → 채택** (§2.0) |

### 채택하지 않은 것과 이유

| 제안 | 낸 쪽 | 기각 사유 |
|---|---|---|
| 카드에 `0 1px 3px rgba(0,0,0,.06)` 그림자 | Gemini | **이미 `card.tsx:41` 에 거의 같은 임의값이 있다.** Tailwind `shadow-sm` 기본값 재추천이라 AI 티를 벗는 수가 아니라 AI 티 그 자체다. 판/바탕 1.08 결정과도 충돌 |
| 1번 시안으로 롤백 | Gemini | before 의 파란 부서칩 + 무지개 아바타 + 4색 컬럼 바가 사람 심사평 「컬러가 너무 다채롭다」의 실체다. `피드백.md` 를 못 본 판단 |
| 진행중=파랑 / 검토=앰버 소프트 틴트 | Gemini | 이 프로젝트가 근거를 적고 버린 5색 체계다. 또한 「지연 = 연분홍 배경 + 진한 레드」와 「완료 = 회색」은 **이미 구현·측정돼 있다**(5.34:1). 없는 것을 고치라는 지적 |
| 파스텔 아바타 | Gemini | 소비자 SaaS 수입품. 출처가 없고 색 갈래만 늘어난다 |
| 「Linear·Jira·Notion 문법에 가깝다」 | Gemini | 이것이 벗으려는 AI 티다. 모든 모델이 기본으로 집는 레퍼런스이고, 화성시 내부 문서 도구가 Linear 처럼 생기면 관점이 없는 도구가 된다 |
| 카드 구조가 반복돼 인공적이다 | GPT | 구조 반복은 훑어 읽기의 전제다(§5.2). AI 티는 구조 반복이 아니라 무게 반복에서 온다. GPT 자신도 §8 에서 사실상 반대로 말한다 |
| 컬럼 헤더 2줄(`대기` / `5건`) | GPT | 제안자 본인이 「당장 바꿀 필요 없다」고 했다. 세로 공간만 먹는다 |
| 부서 칩 파란색 복구 | — | 이미 회색으로 바뀌었고 Gemini 도 회색을 권했다 |

### 이 대조에서 얻은 것

「AI 가 만든 티」를 없애는 수와 **더하는 수**는 겉보기에 같다.
배너를 키우는 것도 더하기이고, 그림자를 넣는 것도, 파스텔을 칠하는 것도 더하기다.
지금까지 나온 모든 처방 중 **빼는 수**는 GPT 의 「배너를 줄여라」 하나뿐이었다.

> 위계는 **더해서** 만드는 것이 아니라 **등급을 매겨서** 만든다.
> 무엇을 크게 할지 정하기 전에, 무엇을 작게 할지부터 정한다.

---

## 15. 전 화면 훑기에서 나온 것 (2026-08-23)

T11~T14 를 넣은 뒤 일곱 화면을 규칙별로 다시 훑었다. 여섯 건이 나왔고
**넷은 이 문서가 예상하지 못한 것**이라 규칙 쪽을 손봤다.

| # | 무엇 | 어디 | 왜 규칙 위반인가 | 한 일 |
|---|---|---|---|---|
| 1 | 문서 등급 배너 + **자기 자신으로 가는 링크** | `approvals/page.tsx` | §5.1. 화면에서 가장 큰 것이 눌러도 아무 일이 없었다 | 배너 → 한 줄, 문서 등급을 결재 목록으로 |
| 2 | 채운 파란 「읽기 전용」 판이 문서보다 무겁고 위에 있다 | `approvals/page.tsx` | §5.1(경고 상자는 여백) | 목록 화면에서만 한 줄로. 폼 화면은 판 유지 |
| 3 | 안 눌리는 숫자가 **파랑** | `handover` 통계 4개 · `login` 근거 3개 | §2. 파랑 = 누를 수 있는 것 | 먹색(`gray-90`) |
| 4 | 겹친 아바타가 **앞 글자를 가린다** | `avatar.tsx` `-ml-2` | 「사람을 가르는 일은 이름 글자가 한다」의 전제가 깨진다 | 겹침 8px → 4px |
| 5 | 「나」가 화면마다 다른 색 (아바타 주황 / 칩 파랑) | `member-list` · `comment-thread` | §2.0 을 넣고 나니 같은 뜻에 색이 둘이 됐다 | 칩도 주황으로 |
| 6 | 사다리 밖 글자 크기 8곳 (`11px`·`12px`) | 칩·아바타·타임라인 | §4 | 전부 `text-body-xs`(13px) |

**4번이 이번에 가장 값싸게 큰 것을 고친 자리다.** 반단계(`-ml-1.5`)를 걷어낼 때
방향을 **올려서**(`-ml-2`) 잡았는데, 24px 원에 13px 글자를 넣고 뒤 원이 ring 2px
을 달고 8px 을 덮으면 글자의 3분의 1이 사라진다. 8배로 확대해 보면
「서 지 민 태」가 **「ㅅ ㅈ ㅁ 태」** 로 읽힌다. 아바타에서 색을 뺀 근거가
「이름 글자가 사람을 가른다」였으므로, 그 글자가 가려지면 §2.0 전체가 무너진다.

> 규칙을 지키는 수(반단계 없애기)에도 방향이 둘 있다. **규칙만 보고 고르면
> 반은 틀린다** — 4px 사다리는 「8로 올려라」가 아니라 「4의 배수로 가라」였다.

**남긴 것과 이유**

- **인계 대기 화면은 「문서」를 선언하지 않는다.** 넘길 것을 고르기 전의 화면이라
  1등이 아직 없다. 억지로 세우면 §5.1 이 막은 통계 타일이 그 자리에 온다.
- **상태 배지의 명도 순서는 문서와 어긋나 있다** — 적힌 순서는 진행중 > 검토 >
  대기 > 완료인데 실측은 13.17 / 7.07 / **7.68** / 5.57 로 대기가 검토보다 세다.
  고치지 않는다. 글자에는 4.5:1 바닥이 있어서 옅은 바탕 두 종류 위에 네 단계를
  욱여넣으면 두 칸이 반드시 붙는다. 열 머리의 **점**은 비문자라 3:1 만 넘으면
  되고, 그래서 거기서는 네 단이 온전히 벌어진다(3.67 / 5.13 / 7.07 / 13.17).
- **`Card`·`CardPad` 의 `doc` 가지는 아직 아무도 안 부른다.** 문서 등급 7곳이
  전부 `<article>`·`<header>`·`<ul>`·`<Link>` 라 `<div>` 컴포넌트를 못 쓴다.
  지우지는 않았고, 대신 거기 잠복해 있던 320px 두 줄 버그(T7 과 같은 것)를
  먼저 막아 뒀다.

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | UNAVAILABLE | codex 401 — `codex login` 필요 |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 0 | — | — |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | ISSUES_OPEN | score 5/10 → 8/10 목표, 결정 4건 확정, 과제 14건 |
| 외부 평가 (Gemini / GPT) | 사용자 제공 | 같은 두 장에 대한 독립 평가 | 2 | 대조 완료 | 일치 2건 채택, 불일치 7건 기각 — §14 |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

**7개 패스 점수 (현재 HEAD `18a5de1` 기준)**

| 패스 | 점수 | 근거 |
|---|---|---|
| 1 정보 구조 | **5/10** | 홈 9/10, 나머지 6화면 3/10. 보드·결재함·인계는 정적 이름표가 32px로 1등 |
| 2 상태 표현 | **4/10** | `loading.tsx` 0 · route `error.tsx` 0 · `Suspense` 0. 빈 화면은 `HandoverStandby` 하나만 뛰어남 |
| 3 여정·감정 | **6/10** | 인계 대기 화면은 본보기. 결재 상세는 되돌릴 수 없는 상신이 15px |
| 4 AI 티 | **5/10** | 상자 1종 34회 · 선 굵기 1종 · 19px 단일 등급 17곳. 반대로 그라디언트·이모지·보라 **0건** |
| 5 디자인 시스템 | **3/10 → 8/10** | DESIGN.md 부재였음(이 문서로 해소). 토큰 5축 중 간격·깊이·선굵기 **3축 없음** |
| 6 반응형·접근성 | **8/10** | 대비 29/29 · `aria-*` 321 · 44px 51곳 · 포커스 유지 · 스크립트 끈 시험. 흠: `approvals/[id]` h1 단계 누락 |
| 7 미해결 결정 | **2건 확정 / 3건 열림** | 아래 |

**확정된 결정**
1. 디자인 태도 = **관공서 문서 언어로 통일** (종이가 진짜다 — §0)
2. 팔레트 = **값 유지, 구조만 재설계** (BI 출처·실측·시험이 가장 큰 자산 — §2)
3. 화면의 「문서」는 **누를 대상**이어야 한다 — 요약 배너·통계 타일은 아무리 커도 여백 등급 (§5.1)
4. 아바타는 **「나」에게만 색** — 사람별 색은 24px에서 안 읽히고 인원이 늘면 거짓이 된다 (§2.0)

**자기 정정 (이전 판에서 틀렸던 것)**
- §9 는 실눈 시험 지배도 ≥ 1.5 를 최종 관문으로 삼았다. **불충분하다** —
  상단에 큰 상자를 얹으면 통과한다. 업무 보드가 실제로 그렇게 통과했다.
  §9.1 에 「1등 칸이 문서 요소 위에 있을 것」 조건을 추가했다 (T13).
- **그 §9.1 의 구현 지시도 틀렸다.** 「1등 칸 중심이 문서 사각형 **안에**」로
  짜서 돌리니 다섯 화면 중 셋이 실패했는데, 원인은 화면이 아니라 지표였다 —
  이 격자가 세는 것은 시선이 아니라 **잉크**이고, 촘촘한 목록이 넓은 흰 판을
  언제나 이긴다. 긍정형을 부정형으로 뒤집고(§9.1 아래), 판정을 격자 해상도
  (228px)에 맞췄다. **문서를 고치기 전에 시험을 돌려 봐서 알아낸 것이다.**
- §5.1 의 「한 화면에 문서는 하나」는 개수가 아니라 **종류**의 규칙이어야 한다.
  보드에서 지연 카드가 두 장이면 문서 등급도 두 개다 (§5.1 아래).

**검증하고 기각한 것**
- `editor.css:772` 의 「중괄호 안 닫힘 → 44px 터치 목표 죽음」 지적 → **사실 아님.**
  파일 중괄호 균형 0, `.ilm-xbtn{min-height:44px}`(786행)은 `@media (pointer: coarse)`
  안에 올바로 들어 있다. 들여쓰기만 어긋나 있다.
- 「디자인 가드레일이 배선 안 됨」 → 세션 중 커밋 `18a5de1` 이 해소함
  (`test:contrast` `check` 진입, `test:squint` 스크립트 등록, 3파일 추적 시작).

**CODEX:** 사용 불가 — `401 Unauthorized (Missing bearer)`. 외부 의견은 Claude 단독 `[single-model]`.

**실측 (2026-08-23, 1440×1000, `check` + `check:visual`)**

| 시험 | 결과 |
|---|---|
| `test:contrast` | **38/38** |
| `test:design` | **8/8** |
| `test:squint` | **5/5** — 홈 지배도 1.66 ✓, 다섯 화면 전부 자리 검사 통과 |
| 실눈 자리 검사 음성 대조 | 보드에 300px 가짜 배너 주입 → 문서 윗변 354 → 674, **통과가 실패로 뒤집힘** ✓ |
| `typecheck` | 통과 |
| `lint` | 오류 39건 — **전부 `rich-doc-editor.tsx`·`use-collab.ts`(이번 작업 밖, 기존)**. 이번에 고친 23개 파일에는 0건 |

**VERDICT:** DESIGN REVIEW COMPLETE, IMPLEMENTED — 결정 4건 확정, 과제 **T1~T14 전부 반영**,
전 화면 훑기에서 6건 추가 수정(§15). eng review required before ship.

**UNRESOLVED DECISIONS:**
- `rule-frame` 을 `gray-40`(2.95:1, 장식으로 간주) 로 갈지 `gray-50`(4.32:1, 입력칸과 충돌) 로 갈지 — §3 각주
- 협업 커서의 사람별 색을 없애고 이름표+먹색으로 갈지, `--cursor-*` 로 이름만 줄지 — §2.1
- 인계 **대기** 화면에 「문서」를 세울지, 세우지 않는 화면으로 남길지 — §15
- `Card`·`CardPad` 의 안 쓰이는 `doc` 가지를 지울지 남길지 — §15 (§5 의 「등급이 있는데 안 쓰이면 없는 것이다」와 충돌)
